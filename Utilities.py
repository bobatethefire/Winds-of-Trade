###Utility functions that I might need here and there
intList = ['1','2','3','4','5','6','7','8','9','0']

#Returns True if the character is an int
def isInt(character):
    return intList.count(character) != 0
